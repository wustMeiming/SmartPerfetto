{{base}}

## 截断恢复补充

前文保留模型已经输出的证据链；以下收尾只基于当前可用的结构化阶段和已记录的假设状态补齐，不引入新的未验证结论。

{{recoveredSections}}

### 阶段证据校准

{{phaseEvidence}}

### 假设验证状态

{{hypothesisEvidence}}

### 收尾建议

- 优先修复已确认且直接影响分析目标的瓶颈；修复后用同一 trace 场景和相同采集口径做回归对比。
- 对已排除的 Binder、GC、锁、IO、频率等方向保留监控，不把缺证据的候选项写成根因。
- 如需代码 owner 级定位，应在已确认瓶颈对应的 trace 区间或已授权源码符号上继续下钻。

## 置信度/限制

本恢复小节不会独立提高置信度。任何未完成、超时、缺失或低置信度阶段仍是报告限制；恢复文本只修复被截断的展示。
