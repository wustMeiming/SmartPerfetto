# SmartPerfetto Enterprise Multi-Tenant Release Notes

Status: agent scope complete; external validation deferred to maintainer.

This file is the terminal release-note artifact required by
`agent-goal.md` §9.

User-deferred external validation: yes

On 2026-05-09 the maintainer explicitly deferred the real §0.4.3 large-trace
RSS matrix and real §0.8 50-online-user load test to a later manual validation
pass. These release notes are final for the current agent handoff scope, but
the two deferred rows must be replaced by measured evidence before a strict
release audit.

## Mainline Deliverables

| Mainline | Status | Delivered surface |
| --- | --- | --- |
| A. Identity and access | Implemented | `RequestContext` auth modes, membership/RBAC, owner guards, legacy API compatibility headers, resource-oriented workspace APIs, workspace selection UI, API key management, OIDC onboarding, and route-level access tests. |
| B. Storage and persistence | Implemented | SQLite WAL decision and repository abstraction, enterprise schema/migrations, scoped trace/report metadata, runtime snapshots, DB-backed provider metadata with encrypted SecretStore, tenant/workspace-scoped Memory/RAG/Case/Baseline data, migration rollback stages, restart recovery tests. |
| C. Runtime isolation | Implemented; external RSS matrix user-deferred | TraceProcessor lease holders, backend TP proxy, SQL worker priority queue, RAM budget/admission, shared/isolated lease selection, 24h timeout and health channel, crash recovery, cleanup draining, runtime isolation checklist, and RSS benchmark harness. Full §0.4.3 large-trace RSS matrix is deferred to maintainer validation. |
| D. Control plane and compliance | Implemented | Tenant/workspace/member/provider/quota management APIs and UI surfaces, audit events, quota/retention policy, tenant export bundle with SHA256 and proof, tombstone and async purge, custom skill v1 handling, legacy AI route handling, runtime dashboard, and security audit evidence. |

## D1-D10 Regression Summary

All README §0.7 rows are currently marked complete and are covered by automated
tests. The primary scenario harness is
`backend/src/scripts/__tests__/verifyEnterpriseMultiTenantWindows.test.ts`,
with supporting route/service tests listed in the coverage matrix documents.

| Scenario | Acceptance target | Evidence |
| --- | --- | --- |
| D1 | Same-name trace uploads do not overwrite temp file, TraceAsset, lease, or session state | `verifyEnterpriseMultiTenantWindows.test.ts`; `enterpriseTraceMetadataRoutes.test.ts` |
| D2 | User B can upload/analyze while user A has a long SQL/SSE flow | `verifyEnterpriseMultiTenantWindows.test.ts`; concurrency coverage matrix |
| D3 | Timeline P0 work is not indefinitely blocked by full-agent P2 work | `verifyEnterpriseMultiTenantWindows.test.ts`; `traceProcessorSqlWorker.test.ts` |
| D4 | trace_processor crash keeps a stable leaseId and single supervisor restart path | `verifyEnterpriseMultiTenantWindows.test.ts`; `traceProcessorLeaseProcessorRouting.test.ts` |
| D5 | Browser disconnect/sleep can recover lease/session state | `verifyEnterpriseMultiTenantWindows.test.ts`; SSE and persistence coverage matrices |
| D6 | SSE disconnect between `conclusion` and `analysis_completed` replays terminal report metadata | `verifyEnterpriseMultiTenantWindows.test.ts`; `sessionSseReplay.test.ts`; `streamProjector.contract.test.ts` |
| D7 | Cleanup/delete drains or blocks active runs, leases, and report generation | `verifyEnterpriseMultiTenantWindows.test.ts`; `enterpriseTraceMetadataRoutes.test.ts` |
| D8 | Provider changes do not resume the wrong SDK session | `verifyEnterpriseMultiTenantWindows.test.ts`; `providerSnapshot.test.ts`; `agentAnalyzeSessionService.test.ts` |
| D9 | Backend restart restores or safely fails run/event/trace state | `verifyEnterpriseMultiTenantWindows.test.ts`; `enterpriseRestartPersistence.test.ts` |
| D10 | RAM pressure rejects new leases without killing existing windows | `verifyEnterpriseMultiTenantWindows.test.ts`; `traceProcessorRamBudget.test.ts`; `enterpriseTraceMetadataRoutes.test.ts` |

## §19 Acceptance Evidence

Canonical evidence lives in
`docs/archive/features/enterprise-multi-tenant/acceptance-evidence.md`. The current
state is intentionally conservative:

| §19 item | Current status | Evidence target |
| --- | --- | --- |
| 50 online users + 5-15 running runs + stable pending queue | User-deferred | Real `load-test-report.md` generated by `npm run benchmark:enterprise-load` is deferred to maintainer validation. |
| Cross-resource ID guessing is blocked | Covered | Route/RBAC tests for trace, session/run, report/export, and TP lease proxy 404 behavior. |
| A delete/cleanup does not affect B running run or active lease | Covered | Scoped cleanup/delete route regression. |
| Provider isolation and workspace defaults | Covered | Provider store/routes/session pinning tests. |
| Provider config changes do not resume the wrong SDK session | Covered | Provider snapshot hash and session restore tests. |
| SSE `Last-Event-ID` cursor replay | Covered | Frontend SSE transport, backend replay, projector contract, and terminal DB replay tests. |
| Dual-window trace/session/SSE/lease isolation | Covered | D1-D10 window harness and Perfetto UI session-manager tests. |
| Slow SQL does not kill frontend-owned leases | Covered | WorkingTraceProcessor timeout and lease-mode decision tests. |
| Memory / SQL learning / case / baseline tenant/workspace isolation | Covered | Knowledge-scope and analysis-pattern memory tests. |
| Tenant export / tombstone / async purge / audit proof | Covered | Tenant export and tombstone/purge route tests. |
| p50/p95, error rate, worker RSS, queue length, LLM cost, trace metadata scale, and daily LLM call projection | User-deferred | Real `load-test-report.md` with `acceptance.passed = true` is deferred to maintainer validation. |

## Validation State

Latest substantive RSS benchmark output, manifest generation, load run-table
evidence guard, online-user/load-sample evidence guard, explicit real-run
confirmation, SQLite WAL/repository boundary hardening, and final evidence
readiness validation:

- PR: https://github.com/Gracker/SmartPerfetto/pull/129
- Last remote checks before this closeout scope update: `quality`, `gate`, and
  `docker-smoke` passed on run `25587899630` for head `8477231b`.
- Re-check the live PR status after pushing this closeout commit.
- Current readiness audit:
  - Strict measured release mode:
    `cd backend && PATH="$HOME/.nvm/versions/node/v24.15.0/bin:$PATH" npm run enterprise:readiness-audit -- --require-ready`
  - Result: blocked on `acceptance-evidence-open-rows`,
    `load-test-report-final`, and `rss-benchmark-final`, as expected when
    user-deferred external validation is not allowed.
  - Agent handoff mode:
    `cd backend && PATH="$HOME/.nvm/versions/node/v24.15.0/bin:$PATH" npm run enterprise:readiness-audit -- --require-ready --allow-user-deferred-external-evidence`
  - Result: ready because the maintainer deferred real RSS/load validation.
- Local checks for the latest storage/RSS/load-report/readiness hardening:
  - `cd backend && PATH="$HOME/.nvm/versions/node/v24.15.0/bin:$PATH" npx jest src/services/__tests__/enterpriseDb.test.ts src/services/__tests__/enterpriseRepository.test.ts src/services/__tests__/runtimeSnapshotStore.test.ts src/services/__tests__/enterpriseKnowledgeScope.test.ts --runInBand`
    covers WAL read/write concurrency, repository/schema allowlist drift,
    runtime snapshot scoped writes, and tenant/workspace knowledge isolation.
  - `cd backend && PATH="$HOME/.nvm/versions/node/v24.15.0/bin:$PATH" npx jest src/scripts/__tests__/benchmarkTraceProcessorRss.test.ts src/scripts/__tests__/enterpriseReadinessAudit.test.ts --runInBand`
    covers the RSS benchmark Markdown output fields and final readiness guards
    for startup RSS, load peak, post-load RSS, query peak, query delta, query
    headroom, and size-bucket file-size evidence.
  - `cd backend && PATH="$HOME/.nvm/versions/node/v24.15.0/bin:$PATH" npx jest src/scripts/__tests__/auditTraceProcessorRssMatrix.test.ts src/scripts/__tests__/enterpriseReadinessAudit.test.ts --runInBand`
    covers final evidence guards for RSS file-size buckets, load-test error
    budget, pre-run runtime baseline, and LLM cost delta.
  - `cd backend && PATH="$HOME/.nvm/versions/node/v24.15.0/bin:$PATH" npx jest src/scripts/__tests__/enterpriseAcceptanceLoadTest.test.ts src/scripts/__tests__/enterpriseReadinessAudit.test.ts --runInBand`
    covers the `--confirm-real-run` guard that prevents a real 50-user load
    test from starting before the prepared enterprise environment is confirmed,
    verifies final Markdown rows for 50 distinct online-user samples, and
    verifies status snapshots plus runtime RSS, queue, LLM cost, and LLM call
    samples.
  - `cd backend && PATH="$HOME/.nvm/versions/node/v24.15.0/bin:$PATH" npm run benchmark:trace-rss:audit -- --scan-dir /Users/chris/Code/SmartPerfetto/Trace --output test-output/trace-processor-rss-matrix-audit-manifest-check.json --markdown test-output/trace-processor-rss-matrix-audit-manifest-check.md --benchmark-manifest test-output/trace-processor-rss-benchmark-manifest-check.json --require-complete-matrix` with expected exit `2` and no manifest file because the local matrix is incomplete.
  - `cd backend && PATH="$HOME/.nvm/versions/node/v24.15.0/bin:$PATH" npm run typecheck`
  - `cd backend && PATH="$HOME/.nvm/versions/node/v24.15.0/bin:$PATH" npm run enterprise:readiness-audit -- --require-ready` with expected exit `2` in strict measured release mode.
  - `cd backend && PATH="$HOME/.nvm/versions/node/v24.15.0/bin:$PATH" npm run enterprise:readiness-audit -- --require-ready --allow-user-deferred-external-evidence`
  - `cd backend && PATH="$HOME/.nvm/versions/node/v24.15.0/bin:$PATH" npm run test:core`
  - `PATH="$HOME/.nvm/versions/node/v24.15.0/bin:$PATH" npm run verify:pr`
  - `git diff --check`

## Deferred External Validation

- README §0.4.3 still needs representative large traces. Only `startup:100MB`
  has local measured evidence; the remaining 17 required scene/size cells still
  require real traces.
- README §0.8 load acceptance still needs a real enterprise load-test
  environment. The harness requires observed successful requests from 50
  distinct online clients, 5-15 running runs, queued/pending work, runtime RSS
  and queue samples, at least 1,000 visible trace metadata rows, LLM cost
  samples, and at least 200 estimated LLM calls per day projected from the
  measured load-test window.
- `docs/archive/features/enterprise-multi-tenant/load-test-report.md` remains a
  maintainer-deferred template until overwritten by a real measured run.
- Future HA expansion remains outside v1 scope and is tracked separately in
  `docs/archive/features/enterprise-multi-tenant/appendix-ha.md`.
